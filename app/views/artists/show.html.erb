<h1><%= @artist.name %></h1>
<% if @artist.song_count > 0 %>
    <h3> Songs: </h3>
    <ul>
    <% @artist.songs.each do |song| %>
        <li><%= link_to song.title, song_path(song) %></li>
        <% end %>
    </ul>
    <% else %>
    <h4>This artist has no songs</h4>
    <% end %>